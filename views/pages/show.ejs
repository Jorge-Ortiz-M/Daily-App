<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head', {title: "New"}); %>
    </head>
    <body>
        <%- include('../partials/header', {date: current_date}); -%>
        <main>
            <h1>Show Page</h1>
            <div class="note-long">
                <h3><%= note.title %></h3> <br>
                <small>
                    Date: <strong><%= note.date %></strong><br>
                </small> <br>
                <%= note.description %>
                <div class="note-links">
                    <div class="link-back">
                        <a href="/">Back</a>
                    </div>
                    <form action="/delete" method="post" class="link-delete">
                        <button type="submit" name="button" value="<%= note._id %>">Delete</button>
                    </form>
                </div>
            </div>
        </main>
        <%- include('../partials/footer', {date: current_date}); -%>
    </body>
</html>